//Divide and Conquer

function QuickSort () {

    const quickSort = (array, start, end) => {
        // since start has to be before end here's the first step
        if (start >= end) return;

            

    };

}